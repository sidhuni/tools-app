fullnameOverride: postgresql

auth:
  enablePostgresUser: true
  username: admin
  database: chms
  existingSecret: "postgresql-secrets" # This now exists!
  secretKeys:
    adminPasswordKey: postgres_password
    userPasswordKey: user_password

primary:
  resourcesPreset: none

  persistence:
    enabled: true
    storageClass: local-path     # 🔥 IMPORTANT FIX
    accessModes:
      - ReadWriteOnce
    size: 8Gi
    mountPath: /bitnami/postgresql

  service:
    type: NodePort               # OK for local testing

  networkPolicy:
    enabled: false

  pdb:
    create: false

# Disable read replicas for local setup
readReplicas:
  replicaCount: 0

persistentVolumeClaimRetentionPolicy:
  enabled: true
