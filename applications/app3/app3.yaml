apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app3
  namespace: argocd
spec:
  project: default

  source:
    repoURL: http://mycharts
    chart: common-app-chart
    targetRevision: 0.1.0

    helm:
      valueFiles:
        - $valuesrepo/app3/values.yaml

  sources:
    - repoURL: https://github.com/sidhuni/my-checksum-helmchart.git
      targetRevision: main
      ref: valuesrepo

  destination:
    server: https://kubernetes.default.svc
    namespace: app3

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
